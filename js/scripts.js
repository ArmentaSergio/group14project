function redirectToAnotherPage() {
    // Specify the URL of the target webpage
    var targetUrl = '../maps/index.html';

    // Redirect to the target webpage
    window.location.href = targetUrl;
}